name: GitHub Readme YouTube Cards

on:
  workflow_dispatch:
  schedule:
    - cron: "0 * * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: DenverCoder1/github-readme-youtube-cards@main
        with:
          channel_id: UC_-gj2vjlVcrANxRC0r54VQ
